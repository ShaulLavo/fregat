/** Foresight registration names for picker rows, so the intent log can tell them apart. */
export const FILE_PICKER_INTENT_PREFIX = 'file-picker-directory:'

// Foresight counts a row scrolling under a still pointer as hover; that is no intent to open it.
export const SCROLL_INTENT_SETTLE_MS = 150
