[
  {
    "file": "apps/web/src/components/file-picker-dialog.tsx",
    "cause": "hoisted-function",
    "reason": "The key handler calls function declarations defined below it; the compiler cannot rewrite hoisted references yet. Reordering 250 lines of handlers is a separate edit."
  },
  {
    "file": "apps/web/src/features/chat-mode/hooks/use-session-actions.ts",
    "cause": "hoisted-function",
    "reason": "archive() calls reconcileRemoval() declared after it. The hook returns plain async actions and renders nothing, so there is no child to re-render."
  },
  {
    "file": "apps/web/src/features/chat/hooks/use-checkpoint-rewind.ts",
    "cause": "throw-in-try",
    "reason": "A mutation-options hook: the try block throws structured errors the catch logs and rethrows. Nothing render-side to memoize."
  },
  {
    "file": "apps/web/src/features/chat/hooks/use-checkpoint-rewind.ts",
    "cause": "try-finally",
    "reason": "The finally closes the wide event on every exit of the mutationFn. Nothing render-side to memoize."
  },
  {
    "file": "apps/web/src/features/chat/hooks/use-composer-inbox.ts",
    "cause": "rule-suppression",
    "reason": "The effect deliberately omits a dependency and says why at the suppression; the hook renders nothing."
  },
  {
    "file": "apps/web/src/features/editor/components/language-server-references-pane.tsx",
    "cause": "rule-suppression",
    "reason": "documentRevisionKey is an invalidation token for a memo that reads the store imperatively to avoid a re-render storm; the dependency is intentional."
  },
  {
    "file": "apps/web/src/features/file-picker/hooks/use-path-input.ts",
    "cause": "try-finally",
    "reason": "submit() returns early inside the try and the finally clears the pending request on every path; splitting it needs a call-site test first."
  },
  {
    "file": "apps/web/src/features/git/hooks/use-message-file-commit.ts",
    "cause": "rule-suppression",
    "reason": "The effect deliberately omits a dependency and says why at the suppression; the hook renders nothing."
  },
  {
    "file": "apps/web/src/features/search/components/result-editor-virtual-window.tsx",
    "cause": "refs-during-render",
    "reason": "Writes the virtualizer scroll functions into latest-value refs during render; moving the writes into an effect changes when a scroll request sees them and needs a scroll scenario first."
  },
  {
    "file": "apps/web/src/features/search/components/result-file-editor-pool-slot.tsx",
    "cause": "refs-during-render",
    "reason": "Keeps the previous line window in a ref and reuses it during render when the next one is equal, to hold identity stable; the repair is a state-based equal-value cache, measured with the search trace."
  },
  {
    "file": "apps/web/src/features/terminal/components/panel.tsx",
    "cause": "rule-suppression",
    "reason": "The theme effect keys on the palette hash, which is the colors identity; depending on the colors object would re-apply the theme every render."
  },
  {
    "file": "apps/web/src/hooks/use-machine-form.ts",
    "cause": "try-finally",
    "reason": "save() returns early inside the try and the finally releases the pending abort on every path; splitting it needs a call-site test first."
  },
  {
    "file": "packages/tree/src/components/FileTree.tsx",
    "cause": "refs-during-render",
    "reason": "Resets baseline model and composition refs during render when the model prop changes; the repair is the derive-state-from-props pattern across the fork."
  },
  {
    "file": "packages/tree/src/components/FileTreeView.tsx",
    "cause": "manual-memo-not-preserved",
    "reason": "Hand-written memoization the compiler cannot prove equivalent; it guards virtualized rows the compiler cannot reach."
  },
  {
    "file": "packages/tree/src/hooks/useFileTreeFocusSync.ts",
    "cause": "unreorderable-expression",
    "reason": "A useEffectEvent default parameter reads request.behavior, a member expression the compiler cannot reorder; the hook renders nothing."
  },
  {
    "file": "packages/ui/src/patterns/virtual-list.tsx",
    "cause": "incompatible-library",
    "reason": "TanStack Virtual's useVirtualizer returns functions that cannot be memoized safely. This is why the memo() guards on VirtualList rows exist."
  }
]
