# Lifecycle, sidebar, and navigation alignment

Compared Platform `3c9b88c35784e571e706600b0cee8e95a2656f77` with T3 Code `7445aa733ada33e45289e5aa5055f79142556513` on 2026-09-20. Upstream citations below refer to that Git object, **not** the older reference checkout. Read the audit protocol and audit-playbook correctness, coverage, architecture, and finding-format sections before auditing. This is a source audit; no application code, tests, live sessions, or deployment were changed.

## Coverage

| User path                                             | Source result                                                                          | Findings                  |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------- |
| Archive, restore, activity after archive              | Confirmed mismatch at filter, eligibility, event, and navigation layers                | LIFE-01, 02, 10           |
| Settle, unsettle, snooze, wake, pin, unpin            | Server operations partially exist; current web rail does not expose their semantics    | LIFE-03, 04               |
| Drag ordering, shelf moves                            | Pin order reused for all row movement; active order absent                             | LIFE-05                   |
| Automatic settlement and idle provider release        | Absent automatic policy; settle does not release provider                              | LIFE-04, 06               |
| Attention, unread, wake indicators                    | Different classifications and first-visit defaults                                     | LIFE-03, 07               |
| Search across environments                            | Metadata matches across environments; message search only current environment          | LIFE-08                   |
| Project grouping and ownership                        | Repository merging exists, but grouping choices/member references missing              | LIFE-09                   |
| Delete and bulk failures                              | Durable provider cleanup exists; selection/navigation/cleanup UX differs               | LIFE-10, 12               |
| Titles, common row/header actions, recoverable drafts | Rename works; regeneration and several upstream affordances absent                     | LIFE-11, 12               |
| Native/mobile parity                                  | Upstream shares lifecycle helpers with mobile; local native consumers not audited here | Explicit open scope below |

Priority meanings: P0 blocks the reported archive fix; P1 is a substantial behavioral mismatch; P2 is a smaller but confirmed parity gap. Effort includes tests: S hours, M roughly a day, L multiple days. Estimates overlap and must not be summed blindly.

## Findings and execution plans

### LIFE-01 — P0: Enforce archive before every active-list classification

- **Status/confidence:** Confirmed mismatch, HIGH.
- **Evidence:** Upstream `apps/web/src/components/Sidebar.tsx:2549–2554` excludes every archived thread before shelf classification. Upstream `apps/web/src/components/CommandPalette.logic.ts:273` also excludes archived threads for normal thread entries. Local `packages/client-core/src/chat/rail/model.ts:152–156` admits `archived && needs-input` into the active rail; `packages/client-core/src/chat/selectors.ts:145` repeats that exception. `apps/web/src/features/chat-mode/utils/tests/session-rail-model.test.ts:30` explicitly locks in the contrary behavior.
- **Impact:** A successful archive leaves old approvals, plans, failures, or interruptions in the active rail. The same row can belong to both archive and active views.
- **Effort/risk:** S / LOW for filtering; MED for the complete archive policy in LIFE-02.
- **Fix sketch:** Archive is an independent visibility decision. Remove both attention exceptions and replace the contrary test with an archive-by-status matrix.
- **Implementation:** Keep the archived detail/open path (`active-session.ts:68`, `session-controller.tsx:49`) available. Audit callers of both selectors so an explicit archive browser opts in, while active rail, next/previous navigation, and normal command entries remain strict. Do not clear an approval or rewrite attention merely to hide a row.
- **Dependencies:** Ship with LIFE-02; filtering alone does not stop later events from unarchiving the row.
- **Acceptance:** Archive a stopped session with each of approval/input/plan/failure/interruption reasons; it leaves active results and remains openable/restorable in archive. Refresh and reconnect preserve this. Archived matches never appear in active search. Restoring shows the same outstanding content.
- **Verification:** In `apps/web`, run `bun --bun vitest run --project node src/features/chat-mode/utils/tests/session-rail-model.test.ts src/features/chat-mode/utils/tests/archived-auto-pick.test.ts`; extend/run `bun --bun vitest run --project dom src/features/chat-mode/components/tests/session-rail.test.tsx`. Add a live archive-attention scenario and inspect its screenshots.

### LIFE-02 — P0: Align archive eligibility and remove automatic unarchive

- **Status/confidence:** Confirmed mismatch, HIGH. This **corrects the earlier conversation's recommendation to retain local automatic unarchive**.
- **Evidence:** Upstream `apps/server/src/orchestration/decider.ts:435–455` checks existence/non-archive only; its UI blocks `session.status === running && activeTurnId != null` at `apps/web/src/hooks/useThreadActions.ts:258`. Local `apps/server/src/orchestration/decider.ts:109–114` calls `requireSettleable`, which refuses all pending requests, queued/adopted starts, and live runtimes (`command-invariants.ts:111–155`); local UI also blocks via `hasRunningTurn` (`use-session-actions.ts:54–60`).
- **Activity evidence:** Upstream's **only** orchestration emission of `thread.unarchived` is explicit `thread.unarchive` (`decider.ts:458–477`; confirmed with `git grep 'thread.unarchived'` over `apps/server/src/orchestration`). Session changes at `1864–1912` reset only settlement for starting/running; approval/input activity at `2143–2194` resets only settlement; user turn start at `1459–1500` clears settlement and snooze. Local `lifecycle-events.ts:14–34` resets archive, settle, and snooze together, called on runtime starting/running/waiting/error (`decider.ts:673–677`), approval/input/error activity (`700–705`), unimplemented plan (`866`), and recovery (`runtime-decider.ts:92`).
- **Impact:** Local archive rejects actions upstream accepts and silently reverses a user's archive after unrelated late events or recovery. Preserving the helper would leave a second archive bug after LIFE-01.
- **Effort/risk:** M / HIGH because the helper crosses provider recovery, plan, and turn paths.
- **Fix sketch:** Split archive, settlement reset, and snooze reset policy by event origin; only explicit unarchive clears archive. Match upstream archive UI and command eligibility rather than sharing settle guards.
- **Implementation:** Replace the general reset helper with explicit event-policy functions. Build the event transition table before editing callers: user send clears settle+snooze; provider starting/running clears settle; approval/input clears settle but retains snooze; provider error/plan does not undo an explicit archive. Verify direct commands as well as UI. Preserve durable turn-start ownership and runtime recovery; do not replace those mechanisms with upstream's timestamp heuristic merely to match source shape.
- **Acceptance:** Compare UI and direct API separately for starting, waiting, running-with-active-ID, running-without-active-ID, and terminal latest-turn states. UI blocks exactly running runtime plus non-null active turn; direct archive accepts every existing nonarchived session. A fresh duplicate archive command fails the nonarchived invariant; receipt replay of the same command is a separate deduplication case. Archive preserves request, settle and snooze fields and does not implicitly stop the provider. Later successful/failed completion, approval/input, plan, startup/recovery and duplicate event never unarchive the row through either snapshot or event paths. Explicit restore does and retains outstanding content. Local `utils/running-turn.ts:6–9` must be changed along with the server policy because it currently adds waiting/latest-turn-running guards.
- **Verification:** In `apps/server`, extend/run `bun --bun vitest run src/orchestration/tests/session-lifecycle.test.ts src/orchestration/tests/decider-invariants.test.ts src/orchestration/tests/session-attention-state.test.ts src/orchestration/tests/provider-runtime-epoch.test.ts`; re-run LIFE-01 browser drive after delivering late provider events through the real test fixture.

### LIFE-03 — P1: Restore lifecycle shelves and separate attention from placement

- **Status/confidence:** Confirmed mismatch/partial implementation, HIGH.
- **Evidence:** Upstream `Sidebar.tsx:2535–2637` classifies non-archived rows as effective snoozed, explicitly settled, pinned, or active, in that precedence. Upstream `Sidebar.logic.ts:812–863` independently classifies approval/input/working/monitoring/failed/ready; a proposed plan or prior interruption alone is not a needs-input shelf. Local `packages/client-core/src/chat/rail/model.ts:101–105,197–230,245–268` renders only needs-input/working/settled sections and drops `pinnedAt`, `settledOverride`, and snooze fields from the view model. Local `apps/server/src/orchestration/utils/session-attention.ts:25–56` gives historic unacknowledged interruption/failure and actionable plans needs-input precedence, while idle immediately becomes settled. Local `session-menu.ts:53–118` has no settle/snooze/pin/unpin actions despite server contracts at `packages/contracts/src/orchestration-commands.ts:157–214`.
- **Impact:** “Settled” locally means no current work, whereas upstream it is an explicit or automatic lifecycle decision. Persisted settle/snooze/pin operations cannot be controlled or understood in the web UI. A completed session immediately moves to history; old errors/plans remain in needs-input.
- **Effort/risk:** L / HIGH; shared projection vocabulary and every consumer must agree.
- **Fix sketch:** Model lifecycle placement independently from runtime/request indicators in client-core, then render the upstream four shelves with owning-environment capability gates and expose actions from both row and stage menus.
- **Implementation:** Inventory all consumers of `attentionState` before changing it. Add pure shelf/status selectors, transport their necessary shell fields, and use one policy in web and any native consumer. Upstream `Sidebar.tsx:2562–2608` requires explicit owner `threadSnooze`/`threadSettlement` support before those shelves apply; unknown descriptors fall through to pin/active, and pin sorting remains capability-independent while mutations/drag are gated. Add snooze presets/custom date-duration picker and precise wake-boundary scheduling. Derive wake state from retained snooze/activity timestamps plus persisted visit acknowledgment (`threadSettled.ts:68–69,115–118,144–167`); do not create a wake flag or timer-triggered server event. Keep project/worktree failures visible as diagnostics without an extra implicit placement rule. Replace obsolete tests that require attention to override archive/settle/snooze.
- **Dependencies:** LIFE-01/02 first; LIFE-04 defines server transitions; LIFE-05 defines order; background-monitoring signals depend on the provider audit.
- **Acceptance:** One fixture contains pinned, active-ready, working, approval/input, failed, settled and snoozed sessions. Running/waiting/tool activity alone does not wake snooze; approval/input raises its hand without clearing stored snooze, and resolution reapplies the derived policy. Failure wakes only when runtime `updatedAt > snoozedAt`; successful completion only when strictly newer, never merely because of interruption. A mere ready status does not settle. Test owners with missing descriptors and settlement support without active reorder, plus descriptor arrival/removal: rows stay reachable, impossible actions remain unavailable, pin order stays stable and one owner's support never enables another. Custom DST-gap times are rejected; tomorrow/next-week use calendar increments (`threadSettled.ts:196–202,286–302`). Reconnect preserves classification. Bulk-snooze outcome and Undo are covered by LIFE-04.
- **Verification:** Extend `session-rail-model.test.ts`, `session-menu.test.ts`, `session-rail.test.tsx`, and a live scenario covering every shelf, timer wake and stale-environment row. Read screenshots, not only assertion results.

### LIFE-04 — P1: Align settle and snooze transitions, including provider release

- **Status/confidence:** Confirmed mismatch, HIGH.
- **Evidence:** Upstream `decider.ts:495–601` lets manual settle dismiss message-mode async questions, rejects native questions/approvals, and emits both unpin and unsnooze companions. Its snooze at `631–674` **allows running sessions** while rejecting pending requests/queued starts. Upstream `Layers/ProviderCommandReactor.ts:1824–1839` reacts to settlement with conditional `thread.session.stop` (`onlyIfSettled: true`), with stale-stop rejection in `decider.ts:1837–1846`. Local `command-invariants.ts:126–131` delegates snooze to settle and therefore refuses running work. `decider.ts:497–518` refuses all requests and clears pin only, leaving snooze intact. `provider-command-reactor.ts:44–55` does not consume `session.settled` at all.
- **Impact:** Snooze cannot hide background work while it continues; settling a snoozed session does not actually remove snooze state; idle providers remain attached after settlement; async questions cannot be dismissed by the upstream action.
- **Effort/risk:** L / HIGH, especially the stop-versus-new-turn race.
- **Fix sketch:** Give archive/settle/snooze separate eligibility functions. Implement upstream companion events, async-question dismissal, and guarded provider release.
- **Implementation:** Introduce request response-mode-aware settle eligibility rather than relying only on count fields. Resolve async message requests without fabricating an answer. Clear snooze and pin in the same event batch. Match upstream's `onlyIfSettled` check at stop-command decision, then verify actual command/event/provider ordering; that check alone does not prove protection against every later external-stop race (`ProviderCommandReactor.ts:1700–1718`). Maintain original timestamps on duplicate commands. Retain snooze data on provider-only activity. Implement bulk snooze as upstream `Sidebar.tsx:3887–3922`: clear selection before outcomes, summarize success/failure counts, and offer Undo only for successfully snoozed targets. Do not apply bulk-delete failed-selection semantics to snooze, unpin or settle.
- **Dependencies:** Request response-mode support from provider/composer audit; LIFE-03 for reachable controls.
- **Acceptance:** Running session snoozes without interrupt; approval/native-input cannot snooze. Manual settle dismisses message-mode questions, rejects native questions/approvals, clears pin+snooze, releases an idle provider. Duplicate settle is stable. Re-engagement before the queued stop is decided blocks that obsolete stop; exercise later provider-intent ordering rather than assuming an epoch guarantee. Restart/replay completes release once. A partially successful bulk snooze clears selection, reports exact successful/failed counts, and Undo wakes only successful targets; failed targets are not included in Undo.
- **Verification:** Extend `apps/server/src/orchestration/tests/session-lifecycle.test.ts` with a real orchestration/provider-boundary fixture for settlement release and the stop race, then from `apps/server` run `bun --bun vitest run src/orchestration/tests/session-lifecycle.test.ts`. No directly named provider-command-reactor suite was found; add coverage here rather than issuing a nonexistent-suite command. Add DOM bulk-snooze partial-success/Undo cases to `apps/web/src/features/chat-mode/components/tests/session-rail.test.tsx`. Run the same shelf browser scenario as LIFE-03.

### LIFE-05 — P1: Separate active ordering from pinning and implement shelf drag transitions

- **Status/confidence:** Confirmed mismatch, HIGH.
- **Evidence:** Upstream `Sidebar.logic.ts:242–333` plans pin/unpin/settle/unsettle/wake/reorder transitions; `Sidebar.tsx:3570–3689` executes them and retains optimistic placement until all canonical fields settle. Upstream `decider.ts:864–875` validates a distinct active reorder; `packages/client-runtime/src/state/threadSort.ts:244–275` reserves hidden-row keys and materializes keyless neighbors. Local `rail-order-commands.ts:54–93` forbids cross-environment, cross-project and cross-attention drops and writes `pinOrderKey` for every row. `packages/client-core/src/chat/commands.ts:526–537` makes first placement a pin. `rail/session-order.ts:19–24` sorts every status by pin key then creation, including history; upstream `Sidebar.logic.ts:974–985` sorts settled by end timestamp.
- **Impact:** Reordering an ordinary row silently pins it in persisted state. Users cannot drag between shelves, and a recently settled old conversation stays below newer-created history. Hidden snoozed slots cannot be faithfully retained by the current model.
- **Effort/risk:** L / MED-HIGH; field propagation, command contracts, and optimistic acknowledgments.
- **Fix sketch:** Add `activeOrderKey` and the distinct active reorder command end-to-end. Adopt upstream drop semantics and per-shelf order while retaining scoped owner dispatch.
- **Implementation:** Update database/read model/contracts/snapshots/client writers first, then derive shelf order. Preserve snoozed rows' retained pin and active keys when allocating. Implement drop targets for empty shelves. Build a full drop intent that waits for all changed lifecycle and order fields before removing optimism, including multi-key materialization. Never move session ownership to the drop target environment; order writes go to each row's owner. Replace the status-equality restriction with lifecycle/capability rules.
- **Dependencies:** LIFE-03/04.
- **Acceptance:** Drag active row without pinning; pin by dropping on pinned; settle clears pin+snooze; return from settled preserves explicit keep-active semantics; reorder near hidden snoozed keys without collision; partial transport failure rolls back only unconfirmed intent; refresh gives same order. Test two projects/two environments and keyless neighbors. Settled timestamp resolution follows upstream `threadSort.ts:27–47`: valid settledAt, latest valid activity/turn time, then updatedAt; malformed timestamps and ID ties are deterministic.
- **Verification:** Extend/run web `--project dom src/features/chat-mode/components/tests/session-rail-drag.test.tsx`, node order/reorder tests and server lifecycle/order tests. Add live pointer and keyboard drag scenarios covering each destination.

### LIFE-06 — P1: Add server-owned automatic settlement

- **Status/confidence:** Missing feature, HIGH in bounded scope.
- **Evidence:** Upstream `apps/server/src/orchestration/ThreadSettlementPolicy.ts:69–135` uses idle age and linked PR closure/merge while excluding open/unknown PRs, pending work, background liveness, explicit overrides and effective snooze. `ThreadSettlementReactor.ts:90–135` resolves project settings and dispatches `thread.auto-settle` with `snapshotSequence` and activity-derived `settledAt`; `Layers/OrchestrationEngine.ts:175,206` guards snapshot races. Local bounded search `rg -n -i 'auto.?settle|settle.*days|settled.*stop' apps/server/src/orchestration packages/contracts/src/settings apps/web/src/features/chat-mode` finds no corresponding implementation; local command schemas contain manual settle only (`orchestration-commands.ts:157–171`).
- **Impact:** Upstream's active list ages work into settled history and responds to PR closure; local immediate-idle classification supplies neither policy nor the ability to keep a session active.
- **Effort/risk:** L / HIGH; depends on reliable PR state and race guards.
- **Fix sketch:** Port the policy as a pure function and add an owner-server reactor, settings and conditional command.
- **Implementation:** Add registry-backed global/project settings with pinned defaults: `sidebarAutoSettleAfterDays = 3` (null disables) and `sidebarAutoSettleOnMerge = true` (`packages/contracts/src/settings.ts:87,1132–1135`). Implement inactivity first and then linked-PR decisions, retaining all exclusion rules. Evaluate candidates on owner server and validate session-specific change after the captured snapshot, not unrelated global sequence churn. Record actual last-work time as settled time. Add conditional provider release from LIFE-04. Regenerate settings docs.
- **Dependencies:** LIFE-03/04; PR-link and background-liveness support from other audit tracks.
- **Acceptance:** Inactive eligible session settles at last activity, not sweep time; closed/merged PR behavior respects settings; one remaining open or unresolved linked PR blocks settlement; explicit keep-active, effective snooze, fresh requests/background work block it; a concurrent turn/rename/override is handled as upstream's guarded command specifies. Repeat after restart with no duplicate reordering.
- **Verification:** New focused server policy/reactor tests copied by behavior from upstream `ThreadSettlementReactor.test.ts`; real server fixture for snapshot race and restart. Browser scenario observes an actual automatic move, then unsettle prevents a repeated sweep.

### LIFE-07 — P1: Match unread defaults, manual unread, and wake state

- **Status/confidence:** Confirmed mismatch, HIGH.
- **Evidence:** Upstream `Sidebar.logic.ts:635–643` treats never-visited history as read; invalid stored visit as unread. Local `packages/client-core/src/chat/rail/unread.ts:25–29` treats absent seen stamp as unread. Upstream `uiStateStore.ts:250–295` makes visits monotonic and supports mark-unread by a stamp before completion; `threadActionMenu.logic.ts:122` and `Sidebar.tsx:3984–3989` expose single/bulk actions. Local `session-read-store.ts:24–49` only marks seen and permits an older stamp to replace a newer one; `use-mark-session-seen.ts:12–19` never records a visit before the first completion. Upstream `Sidebar.tsx:1112–1128` distinguishes unread and woke, including persistent timer-wake prominence.
- **Impact:** Importing/opening the app lights historical sessions as unread; users cannot deliberately mark one unread; seeing a session before its first completion is not represented as a visit; snooze wake acknowledgment cannot be expressed.
- **Effort/risk:** M / MED; first-visit default affects every existing row.
- **Fix sketch:** Introduce visit/explicit-unread/wake acknowledgment semantics matching upstream and route them through shared helpers.
- **Implementation:** Replace completion-only seen interpretation with the upstream visit model, preserving environment keys and clock handling. Make normal visits monotonic; mark-unread is the explicit exception. Track visits for sessions with no completed turn. Keep timer-wake acknowledgment separate from a casual visit where upstream does. Wire single and bulk actions and integrate archive/settle/send wake clearing. Do not migrate broken historical dev state permanently; reset it if necessary.
- **Dependencies:** LIFE-03 snooze presentation.
- **Acceptance:** Never-visited imported completed session is read. Visit while first turn runs, leave, then complete => unread. Mark unread survives until next meaningful visit. Invalid visit timestamp does not hide new completion. Out-of-order effects cannot roll a visit backward. Timer wake remains prominent until upstream-defined engagement. An acknowledged early wake retains its original triggering timestamp after the scheduled deadline so the indicator does not reappear (`threadSettled.ts:151–164`). Same session IDs on two environments remain isolated; wake remains derived, with no new server event at the deadline.
- **Verification:** Extend client unread tests, web read-storage/rail tests and a live leave-complete-return scenario. Include background-tab visibility in browser validation; static comparison alone does not verify focus semantics.

### LIFE-08 — P1: Search messages on every connected environment represented in the rail

- **Status/confidence:** Confirmed partial implementation, HIGH.
- **Evidence:** Upstream `Sidebar.tsx:2645–2657` builds all connected environment IDs and supplies them to thread search. Local `state/rail-environments.ts:18–34` includes all environment slices, but its only search caller is `components/session-rail.tsx:83`; `hooks/use-session-search.ts:24–36` queries one current environment, using its current query client (`utils/session-search-query.ts:48–50`). Local `state/session-search-store.ts:23–39` can hold scoped results but that capability is not populated concurrently.
- **Impact:** A remote conversation is visible in the rail yet disappears for words found only inside its messages. Switching the active environment changes search results unexpectedly.
- **Effort/risk:** M / MED, particularly stale responses and partial failure.
- **Fix sketch:** Fan out the debounced query to each connected environment's query client, aggregate by scoped session key, and expose partial errors without dropping successful results.
- **Implementation:** Use one query generation across environments; clear results on query change and reject stale-generation responses. Aggregate pending state across relevant queries. Preserve current metadata matching instantly; archive and project scope filter result rows consistently. Keep the existing two-character server-search floor and debounce unless pinned upstream query constants demand a change.
- **Dependencies:** None beyond environment client ownership already present.
- **Acceptance:** Two environments with identical session IDs and distinct hidden message terms return only the correct owner. Searching on environment A finds B's message; B disconnecting leaves A results and reports partial search. Slow old query cannot replace a newer query. Machine/project filters and archive mode do not mix visibility.
- **Verification:** Add a two-client search integration test around `use-session-search` and run existing `session-rail.test.tsx` search cases. Add a live multi-environment search scenario.

### LIFE-09 — P1: Preserve project group membership and configurable grouping

- **Status/confidence:** Confirmed partial implementation, HIGH; impact of existing representative actions requires runtime validation.
- **Evidence:** Upstream `packages/client-runtime/src/state/projectGrouping.ts:12–23,89–140` supports repository, repository_path and separate grouping plus overrides; `283–319` retains all scoped member project references and chooses a preferred-environment representative. Pinned `packages/contracts/src/settings.ts:61–67,448–454` defaults to repository with an empty overrides object. Local `packages/client-core/src/chat/rail/model.ts:171–193` deduplicates solely by project ID and keeps the first owner's ref, while counting rows from all matching project IDs. Local IDs intentionally match repository identity across machines (`apps/server/src/orchestration/utils/repository-ids.ts:8–13`). Local group `archiveAllSessions` uses only that representative's environment (`use-project-actions.ts:48–53`), and delete confirmation counts only its sessions (`62–66`).
- **Impact:** A group displaying sessions on multiple machines carries a single-owner action target; “archive all” does not mean all displayed group sessions. Users cannot separate checkouts or choose upstream grouping behavior. Environment iteration order chooses the representative.
- **Effort/risk:** L / HIGH for destructive group actions; M for grouping presentation.
- **Fix sketch:** Represent a logical group separately from its physical/scoped member references; implement grouping settings and explicit action scopes.
- **Implementation:** Preserve Platform repository/worktree identity in storage. Add grouping view models with member refs, deterministic preferred owner, per-physical order/collapse keys and upstream grouping options. Scope display filters to member refs. Match upstream action target resolution explicitly: group-wide operations enumerate members, physical project operations label/select the physical owner. Never broaden delete silently based on a shared repository ID.
- **Dependencies:** LIFE-03 rail grouping; environment/remote track for capability/settings transport.
- **Acceptance:** Two subprojects of one repository on two environments merge all members in repository mode, group matching repository-relative subpaths in repository_path mode, and remain physical in separate mode. Missing repository identity falls back to physical ownership. Per-project overrides and deterministic preferred representative apply. Scoped actions report which members they change; switching primary environment never chooses an arbitrary destructive owner. A disconnected member remains unavailable rather than silently omitted from an “all” outcome.
- **Verification:** Extend `session-rail-model.test.ts` and `components/tests/project-menu.test.tsx` with two owners sharing project identity. Live two-environment grouping/action scenario, with a non-destructive fixture for the delete preview.

### LIFE-10 — P1: Match navigation after archive/delete and retain failed bulk-delete selections

- **Status/confidence:** Confirmed mismatch, HIGH.
- **Evidence:** Upstream current-thread archive opens a new draft in the same owning project (`useThreadActions.ts:269–295`). Its delete fallback chooses the first surviving project thread in configured sort order (`Sidebar.logic.ts:1119–1142`), and bulk deletion tracks successful entries (`405–428`) then removes only confirmed-gone selections (`Sidebar.tsx:4005–4029`). Local archive/delete share `sessionRemoval` (`use-session-actions.ts:62–90`), whose successor is the adjacent row (`state/removal.ts:22–30`; `utils/session-neighbour.ts:13–16`). Local bulk loops clear every selection after success or failure (`use-session-actions.ts:72–74,79–90`). Local `state/navigation.ts:877–925` applies the same successor to current main/sidebar chat tokens.
- **Impact:** Archive drops the user into another conversation instead of a fresh draft. Delete picks a different fallback. Partial bulk-delete failure destroys the remaining retry selection and can navigate through sessions being deleted later in the same batch. Failed-selection retention is proven for upstream delete only; no bulk-archive parity rule is established by that evidence.
- **Effort/risk:** M / MED; scoped main/sidebar history is sensitive.
- **Fix sketch:** Split archive and delete follow-up plans; accumulate mutation success separately from navigation success and retain unsuccessful bulk-delete selections.
- **Implementation:** Archive current session => owner-project draft. Delete => upstream survivor choice, excluding already-deleted batch IDs. Keep mutation success when navigation fails and display that distinction. Bulk delete clears only confirmed removed IDs and retains failed IDs for retry. Use the pinned `confirmThreadDelete = true` default (`settings.ts:354`). Snooze/unpin/settle clear selection under their own upstream policy (`Sidebar.tsx:3887,3944,3981`); LIFE-04 covers snooze counts and Undo. Any proposed archive failed-selection retention is a local UX addition until separately traced. Carry scoped owner refs throughout. Preserving local safety against overwriting a route change while mutation is pending is an additional safety requirement, **not proven upstream parity**: upstream captures current-route status before awaiting archive (`useThreadActions.ts:269–272,287`).
- **Dependencies:** LIFE-01/02/05 for definitive visible order.
- **Acceptance:** Archive the active middle row => draft; archive background row => route stays. Delete with multiple survivors => upstream first-sorted fallback. In a three-row delete batch with middle failure, successful rows disappear and failed row stays selected. Navigation failure says archive/delete succeeded. Separately prove the added local safety requirement that a route change during pending mutation is not overwritten; do not count it as upstream equivalence. Repeat with same session IDs on different machines.
- **Verification:** From `apps/web`, extend/run `bun --bun vitest run --project dom src/features/chat-mode/components/tests/session-menu.test.tsx src/features/chat-mode/components/tests/session-rail.test.tsx src/features/address/tests/history-target.test.tsx`. Add a new pure removal/fallback test under `src/features/chat-mode/utils/tests/` and run its exact newly created filename with `--project node`; no existing session-neighbour test was found. Add a live delete-batch partial-failure/navigation scenario.

### LIFE-11 — P2: Implement generated title lifecycle and regeneration actions

- **Status/confidence:** Missing feature, HIGH in bounded scope.
- **Evidence:** Upstream `decider.ts:1259–1318` refines generated titles and guards regeneration completion by current request/version; `threadActionMenu.logic.ts:111–122` exposes regenerate with pending state; `Sidebar.tsx:3826,3870` provides eligible bulk regeneration. Local title creation takes a prompt seed (`packages/client-core/src/chat/commands.ts:142,211,545`), and metadata update simply writes a supplied title (`apps/server/src/orchestration/decider.ts:432–445`). Bounded search of `apps/web/src`, `apps/server/src`, `packages/contracts`, `packages/client-core/src` for `title.regenerat|titleRegenerat|regenerat.*Title|title.*generat|generat.*title` found no generated-title request/refinement lifecycle. Local `session-menu.ts:69–97` exposes rename only.
- **Impact:** Upstream automatically improves provisional titles and lets users regenerate; local long-lived sessions retain a prompt-derived/manual title with no equivalent action or concurrency protection.
- **Effort/risk:** L / MED; provider/model dispatch and stale-title overwrite protection.
- **Fix sketch:** Add generated/manual title provenance, versioned generation requests and completion, then expose single/bulk regeneration.
- **Implementation:** Trace upstream initial title-generation reactor before implementing; this report confirms the lifecycle and affordance gap but does not specify model selection policy. Add contract/read-model/projection fields and generation reactor, preserve a manual rename against late generation, support retry and unavailable-provider outcomes, and wire reachable row/header/bulk actions. Update archive-browser title projection too.
- **Dependencies:** Provider capabilities/model-selection track.
- **Acceptance:** Initial prompt receives upstream-style generated/refined title; manual rename during generation wins; an older regeneration result cannot overwrite a newer request; pending/error state is visible and retry works on owner environment. Bulk only targets eligible sessions.
- **Verification:** Focused server title-decider/reactor tests and DOM menu tests; live real title-regeneration scenario with controlled provider fixture. Do not claim provider output quality from a mocked string.

### LIFE-12 — P2: Complete row affordances, draft recovery, and delete-worktree flow

- **Status/confidence:** Confirmed UI gaps, HIGH; session-terminal cleanup mapping remains an investigate item.
- **Evidence:** Upstream `threadActionMenu.logic.ts:134–147` has copy path/branch/thread-ID and project settings; local `session-menu.ts:53–118` lacks them. Upstream `Sidebar.tsx:820–833` renders content-bearing abandoned drafts above pinned threads; local `session-rail.tsx:83,207` has search/new-session wiring and renders the persisted session model, with no draft collection input (`rail/model.ts:136–150`). Upstream `useThreadActions.ts:352–383` offers orphaned worktree deletion unless automatic cleanup is configured; local `session-delete-dialog.tsx:51–66` explicitly retains the checkout and links to a separate manager. Upstream delete stops the provider and closes thread terminals (`useThreadActions.ts:386–395`); local durable cleanup exists (`session-deletion-reactor.ts:79–155`) for provider and attachment blobs, but it has no terminal dependency. Local ordinary terminals belong to worktrees (`terminal/service.ts:174–176,899–901`), so closing all worktree terminals would be incorrect.
- **Impact:** Common upstream actions and unsent-draft recovery require extra navigation or are inaccessible. Deleting the last session requires separate manual cleanup. Terminal ownership must be mapped before promising equivalent release behavior.
- **Effort/risk:** M for actions/drafts, L for cleanup / MED-HIGH because shared worktrees/terminals cannot be deleted by association alone.
- **Fix sketch:** Add the upstream common menu actions and recoverable draft block. Integrate last-reference worktree cleanup with explicit owner and automatic-cleanup settings; investigate which terminals are owned by the deleted session.
- **Implementation:** Reuse the shared row/header action definition. Expose draft identities with owner project/worktree and meaningful content predicate, then navigate to that draft rather than starting a replacement. For deletion, reuse existing safe worktree manager eligibility but offer the upstream inline decision; account for archived and failed-to-delete references. Preserve local durable provider/blob cleanup, which already solves a real retry problem. Inventory agent-specific terminals versus ordinary worktree shells and close only session-owned resources; report cleanup failures separately from a successful session deletion.
- **Dependencies:** Draft/composer track; worktree and settings track; LIFE-10 successful-deletion set.
- **Acceptance:** Copy values match owner paths/IDs; project settings targets owner; leave a draft containing text or attachments and recover it from rail after reload; deleting last managed-worktree session offers correct cleanup, but surviving/archived references prevent removal. Shared worktree shells remain alive; truly session-owned terminal/process state is released and cleanup failure is visible.
- **Verification:** Extend menu tests, draft persistence tests, and `components/tests/worktree-cleanup-eligibility.test.tsx`; add deletion/draft browser scenario. Real in-process cleanup test must assert filesystem and provider state, not only dispatched action names.

## Second pass: negative paths and corrections

1. **Archive activity corrected:** The first-pass intuition that new requests should automatically unarchive is not upstream behavior. Checked all upstream orchestration `thread.unarchived` occurrences and the runtime/activity/turn-start branches, not merely the sidebar filter. LIFE-02 is necessary for hard parity.
2. **Settle provider release added:** Reading the downstream reactor found a stop operation absent from local handling. A settle-only UI port would still leave processes alive. The `onlyIfSettled` guard is part of acceptance, not optional hardening.
3. **Snooze is not pause:** Confirmed running work is snoozable upstream. Rejected local reuse of settle eligibility as an acceptable simplification. Retaining a snooze return timestamp through provider activity is intentional upstream behavior, not stale state to erase.
4. **Bulk-delete failure added:** Traced mutation failure through selection updates and navigation. Local per-row errors exist; the gap is loss of failed delete selections and aggregate follow-up. Independent review narrowed this to delete: upstream snooze/unpin/settle clear selection, with partial-snooze counts and success-only Undo.
5. **Cross-environment search verified as wiring gap:** Local store already scopes matches correctly and tests prevent ID collision; those tests do not demonstrate queries against all visible owners. One mounted search hook remains the missing connection.
6. **Project identity correction:** Shared project IDs are intentional deterministic repository identity, not random UUID collision. The gap is group membership/action modeling and configurable physical separation, not replacing identity generation.
7. **Delete provider leak claim rejected:** Local `SessionDeletionReactor` does stop provider runtime and retry incomplete cleanup at recovery (`53–58,118–135`). Do not replace it with only a frontend pre-delete call. Terminal cleanup remains a mapping question because upstream thread ownership differs from local worktree shell ownership.
8. **Queued-start heuristic difference separated:** Upstream infers a two-minute queued window (`threadSettled.ts:23–44`); local persists explicit queued/claimed/adopted provider ownership (`command-invariants.ts:148–151`). Hard behavioral alignment requires equivalent reachable eligibility/recovery outcomes, not discarding local durable ownership for a timestamp heuristic. Test crash recovery to terminal state before deciding any change here.
9. **Archive detail path confirmed:** Local explicitly selected archived sessions remain openable (`active-session.ts:68`, `session-controller.tsx:49`) and integration tests cover restore, archived worktree ownership, and archive search (`components/tests/session-rail.test.tsx:67–193`). Preserve those paths when fixing active visibility.
10. **Same-branch new session substantially matched:** Local `use-session-menu.ts:32–35` passes the existing base worktree into the draft action. Do not report this missing merely because its label says “New Session in This Project”; verify draft behavior with the composer track.

## Structural differences that are not independent parity bugs

- Platform session identity corresponds to upstream thread identity; provider runtime/session is a child concern. Keep environment/project/worktree ownership explicit. Renaming local symbols to thread is unnecessary.
- Effect atoms versus TanStack/Zustand and Elysia versus Effect server services do not themselves change behavior. Port transition semantics and observable results, not framework architecture.
- Platform's durable deletion reactor can implement upstream outcomes with stronger retry behavior. Source-order equality is not required, but user-visible outcome and resource ownership must match.
- An archive browser in the rail instead of Settings is an extra entry point. The core requirement is strict separation, accessible restore/delete, and correct scoped ownership. Exact Settings placement is still a product parity decision, not silently waived here.

## Independent review reconciliation

Applied all six accepted corrections from `review-lifecycle.md` without changing LIFE IDs: repository_path and verified defaults, owning-environment capability/unknown-descriptor cases, derived wake state with freshness/DST boundaries, delete-only failed-selection parity and actual snooze Undo behavior, executable test paths/new-suite requirements, and the archive UI/API/event matrix. Route-change safety is explicitly an additional local constraint rather than an upstream claim. The provider-stop plan now distinguishes the observed decision-time guard from unproven later external-stop ordering. These are source-review corrections; no tests were run and no runtime parity claim was added.

## Execution order and proof

1. LIFE-01 + LIFE-02 as one archive correctness change, with exact transition matrix and current regression tests replaced.
2. LIFE-04 lifecycle command contract/stop safety, then LIFE-03 shelves/status/actions, then LIFE-05 drag and order.
3. LIFE-07 unread/wake, LIFE-08 multi-environment search, LIFE-09 grouping, LIFE-10 bulk/navigation. Independent pure helpers can be implemented concurrently only with separated file ownership.
4. LIFE-06 automatic settlement once PR/background capabilities exist; LIFE-11 titles and LIFE-12 draft/common actions/cleanup alongside their owning tracks.

For every delivered UI unit: follow `verify-fregat`, reuse the running dev server, add missing scenarios/selectors and feature-map entries, run `bun run agent:browser scenario <registered-name>`, inspect screenshots, and name evidence directory. Do not invent a scenario name and claim it ran. Existing `scripts/agent/scenarios/pattern-lists.ts:85` registers `session-rail` but only opens/selects a row; it does not prove any new lifecycle behavior. After relevant tests and live proof, coordinator follows repository deployment instructions; this audit itself does not deploy.

## Unverified scope and required follow-up

- No running-app reproduction or timing claims were made. Provider races are code-supported risks with concrete test plans, not reproduced failures.
- Mobile/native UI is not proved equivalent. Upstream mobile consumes shared lifecycle/grouping/sort helpers and has thread arrangement/search/action surfaces. Map every LIFE item to local `apps/mac` or explicitly classify that surface as missing; do not infer parity from client-core reuse.
- Upstream provider-specific title model selection and full draft identity/persistence belong to adjacent tracks; LIFE-11/12 require those traces before implementation.
- Verified defaults are recorded in LIFE-06/09/10. Upstream `worktreeCleanup` defaults to null with off/custom modes and explicit rules (`packages/contracts/src/settings.ts:975–989,1051`); trace the shared resolver before treating null as off or deciding effective cleanup eligibility. That resolver behavior remains open.
- Full notification/OS unread behavior, terminal lifecycle ownership, and worktree cleanup policy are shared with provider/platform tracks. The claims here stay bounded to rail/lifecycle source evidence.
