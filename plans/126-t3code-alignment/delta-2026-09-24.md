# Upstream delta audit, 2026-09-24

The acceptance baseline stays at T3 Code `7445aa733ada33e45289e5aa5055f79142556513`. The census,
fixtures and CI pin read that object, and moving it is a separate reviewed change. This record is
the explicit delta the plan requires for newer reference commits.

- Audited upstream: `9383f4ad71285b9f9a6a9b65f2f961ea2cf4308f` (`origin/main`, 2026-09-24),
  149 commits after the pin. Read with `git -C references/t3code show 9383f4ad:<path>`.
- Platform: `c2af88b4` plus the working tree of 2026-09-24.
- Method: a feature survey of post-pin `feat` commits and `docs/user/*`, each candidate then
  checked at both upstream objects and by a bounded local search. Source review only; nothing
  was run in either app.

## New finding groups

"Missed at pin" means the behavior already existed at `7445aa73` and the 2026-09-20 audit did not
record it. "Post-pin" rows cite the delta commit; their acceptance moves to the pinned baseline
only when the pin is bumped.

| ID             | Behavior                                                   | Upstream origin                                          | Wave |
| -------------- | ---------------------------------------------------------- | -------------------------------------------------------- | ---- |
| LIFE-13        | Undo for settle, snooze, archive and unpin, plus `mod+z`   | Post-pin: `5781b524`, `6b0a04ad`, `9a609a4e`             | 2    |
| LIFE-14        | Linked pull-request badge on sidebar rows, background sync | Missed at pin; refined by `9f0c9f72`, `18de6bb3`         | 2    |
| INTERACTION-13 | Start a thread in the background from a new draft          | Missed at pin                                            | 3    |
| INTERACTION-14 | Model picker favorites                                     | Missed at pin                                            | 4    |
| EXT-14         | Keep the default-branch checkout current by fast-forward   | Missed at pin                                            | 5    |
| EXT-15         | Submodule policy for new worktrees                         | Post-pin: `0141bc2b`, `1262d2f3`                         | 5    |
| EXT-16         | Balance new threads across connected machines              | Missed at pin                                            | 6    |
| EXT-17         | Usage page with estimated cost and editable model prices   | Missed at pin; implementation handed to Plan 141         | 4    |
| EXT-18         | Follow worktree branch drift so session branch stays true  | Missed at pin; was rejected as `git-branch-drift-follow` | 5    |

## Folded into existing groups

- **SnapShots** (`apps/desktop/src/snapShot/`, `docs/user/snap-shot.md`): desktop window capture
  into a draft, present at the pin. EXT-09 already owns desktop capture; its evidence now names it.
- **Provider compatibility ranges** (`96c4bfa0`, `7e65b226`): version ranges per harness and shared
  sign-in bindings. RUNTIME-10 owns version verification; Plan 138 owns the Claude binary and its
  reported version. No new group.
- **Pull-request reference parsing** (`apps/web/src/pullRequestReference.ts`): reopened into
  EXT-02, which already requires a PR to open a scoped session.
- **Plain Enter inserts a newline on mobile** (`apps/web/src/composer-logic.ts:28,39`): reopened
  into INTERACTION-11's send-key acceptance. The phone layout itself is Plan 143's discussion.

## Reopened rejections

`docs/t3code-chat-parity-gap-analysis.md` "Deliberate non-parity" rejected four behaviors for
reasons that no longer hold. The plan's authority rule already says earlier local decisions do not
exempt behavior; these rows now point at their owners.

| Rejected row                      | Stale reason                          | Now owned by                                 |
| --------------------------------- | ------------------------------------- | -------------------------------------------- |
| `adjacent-usage-analytics`        | Pricing tables go stale               | EXT-17; upstream prices are user-editable    |
| `git-branch-drift-follow`         | "Revisit only if PR affordances land" | EXT-18; branch PR state and create now exist |
| `adjacent-pull-request-reference` | No start-a-session-from-a-PR flow     | EXT-02, which plans that flow                |
| `composer-mobile-enter-newline`   | "There is no mobile target"           | INTERACTION-11, with Plan 143 for the layout |

## Related plans

Plans 138–145 were written from the same 2026-09-24 survey. Where one overlaps a group here, Plan
126 keeps the upstream acceptance cases and the other plan owns the implementation or a
Platform-specific extension. See the main plan's related-plans table.

## Left out

- Post-pin internal refactors (the no-restyle className wave, #13020–13210), OTLP export
  (`3bb06ad9`, `82059df1`) and middle truncation of paths (`f7efb535`). None changes reachable
  chat behavior, and the last contradicts Platform's truncation rule.
- T3 Connect/Clerk pairing, cookie import and `t3 update`/`t3 service`: already inside EXT-08 and
  EXT-09, unchanged by this delta.
- The mobile app's own post-pin features. EXT-09 still requires a per-OS enumeration first.
