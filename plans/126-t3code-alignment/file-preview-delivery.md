# General files and submitted-answer display

Sent files now render as named file controls in the transcript, separate from image thumbnails. Opening one shows its name, MIME type and size. Text, JSON, XML and YAML files up to 256 KiB load through an owner-URL-keyed TanStack query and render as literal text. Larger files and other formats offer download without a text preview. The download keeps the original filename and targets the owning environment's attachment route. Missing previews surface an error and Retry.

Four focused tests pass: real upload/preview/download bytes and content-disposition through the in-process Bun server, missing-blob rejection, literal text rendering and owner links, and binary-file exclusion from the image lightbox. Happy DOM cannot reproduce Bun.file response bodies, so the route-byte check runs in the node project; DOM checks use query data and do not claim a browser network proof.

Native `user-input.answer-submitted` work rows now expand into original question text, formatted answers and each question's image/file attachments. The question order and recursive answer formatting follow pinned `MessagesTimeline.tsx:5028–5088` and `work-log/userInput.ts:159–164` at `7445aa733ada33e45289e5aa5055f79142556513`. Pending-request folding remains unchanged. Ten ActivityRow tests and two pure history/equality tests pass. Structural equality detects changed answer or attachment metadata while preserving equal derived rows.

The `file-attachments` scenario drives upload, draft recovery after reload, native provider file consumption, transcript preview and exact downloaded bytes. Live execution passed on `20260920T151610Z-b915d3e0-plan126-titles-files`. Evidence: `/work/tmp/fregat-evidence/20260920T152332Z-scenario-file-attachments/`; all four screenshots inspected. The native fixture read exact uploaded bytes, and browser download filename/content matched. Session/provider/settings cleanup completed and both native processes exited. The log window contains unrelated older-scope WebSocket/LSP teardown warnings and errors, so it is not a clean-log claim. An earlier probe failed on using inputValue for a contenteditable textbox; its fixtures were cleaned and the scenario now reads textContent. These checks do not establish complete composer/attachment parity, provider matrix coverage, or native answer-history browser evidence.
