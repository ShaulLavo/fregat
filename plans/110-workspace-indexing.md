# Workspace indexing: what we index, and what should index us

Status: **research done 2026-09-25** (question 7's census was split into [Plan 170](170-language-census.md)). The decision record and follow-up plans are under [Research findings](#research-findings-2026-09-25). Requested 2026-09-13.

A workspace index already exists and is substantial: [`apps/server/src/fs/workspace-index.ts`](../apps/server/src/fs/workspace-index.ts), 1414 lines, a gitignore-aware file index with a watcher, coalesced updates, readiness states, content-kind sniffing and character bags for fuzzy matching. What does not exist is an answer to the question several plans are now queuing behind: **what else belongs in an index, who owns it, and what is allowed to depend on it.**

This is a research plan. It ends in a decision record and a scope, not in code. It exists in `plans/` rather than `docs/` so it stays in the inventory instead of quietly aging out. [Root PLAN.md](../PLAN.md) owns scheduling.

## What is indexed today

| Piece          | Current state                                                                                                                                                      |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Unit           | One entry per filesystem entry: path, basename, extension, size, mtime/birthtime, type and target type, hidden, gitignored, default-ignored, stale flag, version.  |
| Classification | `fileKind` of `source`/`document`/`config`/`image`/`binary`/`other`, and `contentKind` sniffed from the first 512 bytes.                                           |
| Freshness      | A watcher with 25 ms coalescing and a 500-event rebuild threshold, and an explicit readiness state — `cold`, `building`, `ready`, `stale`, `failed`.               |
| Search         | `charBag` per entry supports fuzzy path matching. Content search is separate and shells to ripgrep (`search-rg-parser.ts`) with a fallback (`search-fallback.ts`). |
| Not indexed    | Language identity beyond extension. Symbols. Content. Links between documents. Anything derived from parsing.                                                      |
| Ownership      | Server-side, per workspace, in memory. No persistence across restarts.                                                                                             |

## Consumers waiting on an answer

These are the reason this plan exists. Each one wants something the current index does not hold.

| Consumer                                                             | What it needs                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Grammar and theme prefetch                                           | A language census. The editor currently preloads all 53 Shiki grammars and every theme after first paint (`EDITOR_SHIKI_PRELOAD_LANGUAGES` in [`shiki-languages.ts`](../apps/web/src/features/editor/utils/shiki-languages.ts)). A TypeScript project needs four. VS Code loads a grammar on the first file of that language; we want to be earlier than that without being exhaustive. |
| [Plan 088](088-native-code-intelligence.md) native code intelligence | Semantic retrieval, symbol lookup, project memory. The heaviest consumer and the one most likely to dictate the storage answer.                                                                                                                                                                                                                                                         |
| [Plan 108](108-markdown-modes.md) Obsidian mode                      | A document graph: links between notes, backlinks, tags. Obsidian's vault index, in our terms.                                                                                                                                                                                                                                                                                           |
| Search                                                               | Whether content search stays a ripgrep shell-out or gains an index, and what that would cost to keep fresh.                                                                                                                                                                                                                                                                             |
| Tooling and agents                                                   | What an agent can ask about a workspace without reading every file.                                                                                                                                                                                                                                                                                                                     |

## Questions this plan must answer

1. **One index or several?** A file index, a symbol index and a document graph have different freshness costs, different rebuild triggers and different failure modes. Merging them is a coupling decision, not a convenience.
2. **Where does it live?** In memory per workspace as today, on disk per workspace, or in the existing database under `apps/server/src/db`. Persistence changes startup, invalidation and correctness-after-crash.
3. **What is the freshness contract?** Today's readiness states are honest about staleness. Any new layer needs the same, and consumers need to be able to ask rather than assume.
4. **What is cheap enough to index eagerly?** A language census is a fold over extensions the index already holds — nearly free. A symbol index is a parse of every file. These are not the same decision and should not be made together.
5. **What does it cost on a large repository?** The existing [`bench:workspace-search`](../apps/server/scripts/workspace-search-benchmark.ts) is the precedent: any proposal arrives with a measurement, per [AGENTS.md § Optimization](../AGENTS.md#optimization-and-performance-work).
6. **Who may depend on it?** An index that the editor cannot boot without is a different thing from one that makes the editor faster. The prefetch consumer must degrade to today's behaviour when the index is cold.
7. **Does a language census need its own answer first?** It is the smallest consumer, it is nearly free, and it unblocks a measurable win. It may deserve to be split out and shipped ahead of the rest rather than waiting for the whole design. Decided 2026-09-25: owner — split out as [Plan 170](170-language-census.md); the rest of this research runs later.
8. **Should content classification be ours at all?** `contentKind` is sniffed by hand in [`workspace-index.ts`](../apps/server/src/fs/workspace-index.ts): one 512-byte read, five image magic numbers (PNG, JPEG, GIF, WebP, BMP), then a NUL-and-control-byte heuristic for everything else. [`file-type`](https://www.npmjs.com/package/file-type) recognises several hundred binary formats from the same kind of header read. These are two decisions, not one — it can replace the image arm and give every entry a real MIME type, but it detects **binary** formats only and says so, so the text-versus-binary verdict that most consumers actually read stays ours either way.

## Research steps

1. Read the existing index end to end and write down its actual invariants, not its intended ones — especially what `stale` means to a consumer and when a rebuild is triggered.
2. Survey prior art with the same discipline used for markdown in Plan 107: how VS Code's workspace symbol provider, ripgrep, and an Obsidian vault index each handle freshness and scale. Clone what is readable into the repo's `references/` directory (gitignored; see AGENTS.md "Reference Clones"); `references/vscode` is already there.
3. **Read `file-type`, then decide what we want from it (question 8).** Treat it as source material first and a dependency second. It is `22.1.0`, MIT, ESM-only, `node >= 22`, 138 KB unpacked, four transitive dependencies (`strtok3`, `token-types`, `uint8array-extras`, `@tokenizer/inflate`), and server-side only — nothing here touches the first-load budget Plans 106–109 own.

   The detection is not a framework, which is what makes it minable: `source/index.js` is one linear `fromTokenizer` chain of `check([bytes], {offset})` calls against a peeked buffer that grows only as far as a format demands (3 bytes for gzip, 32 for the common set, more for the deep ones), and `source/supported.js` is a 370-line extension and MIME inventory. Signatures lift out one at a time under MIT with attribution.

   Three outcomes are open, and the study picks **per arm**, not once for the whole package:
   - **Depend** — we get several hundred formats and their maintenance, and a dependency in the index's hot path.
   - **Lift the signatures we can name a consumer for** — their table, our pass, no dependency, and we own the bytes we copied.
   - **Leave it** — the five image signatures we have are all anything asks for today.

4. **Design the escalation, not a shorter read.** The 512-byte prefix is cheap and stays — one read per entry is not the cost worth tuning. The open question is what happens when 512 bytes do **not** settle the answer, which today is nothing: `sniffBytes` returns its best guess from the prefix and the entry keeps it.

   Make pass one return three outcomes instead of two: settled, settled-as-`unknown`, or **undecided with a named reason** — which format family the prefix matched and how many more bytes that family needs. Only the third class earns a second read, and the reason is what bounds it.

   The escalation tiers are not uniform, and `file-type` is the evidence for what each costs:
   - **A longer prefix.** Some formats just want more header — their stream helper samples 4100 bytes by default, and ISO 9660 wants ~32 KiB. A second bounded read, nothing more.
   - **A walk.** Zip containers are the expensive tier and the common one: `.docx`, `.xlsx`, `.odt`, `.jar`, `.apk` and `.epub` are all `PK\x03\x04` in the first four bytes, so the prefix can tell you _it is a zip_ and never which one. `detectors/zip.js` is 648 lines that walk local file headers forward looking for the entry that names the format, with explicit `maximumUntrustedSkipSizeInBytes` and `maximumZipEntrySizeInBytes` caps. That shape — bounded walk, hard caps, give up and stay generic — is the part worth copying whatever we decide about the package.
   - **Give up deliberately.** `unknown` after a capped escalation is a real verdict and must be storable as one, or the next cold build pays the same cost again for the same file.

   Then cap it, because an index escalation is a per-entry cost on a cold build: how many entries in a real repository reach pass two at all, what the tail looks like, and what the ceiling is before the build is simply slower for a distinction nobody reads.

   Two things to keep straight while doing it:
   - **Ordering inside pass one is a correctness matter, not a speed one.** A NUL settles `binary`, but every image has a NUL early — so signatures must run before the NUL scan, as `sniffBytes` already does. Any reshuffle preserves that or silently deletes the `image` arm.
   - **Escalate only for a distinction a consumer asked for.** `.docx`, `.jar` and `.iso` all land on `binary` today and nothing complains. If no consumer wants better, the correct escalation budget is zero — and recording _that_, with the tiers priced so a future consumer can reopen it cheaply, is a complete answer to this step rather than a dodge.

5. **Cost and correctness, on a real large repository.**
   - **Correctness first.** Run the candidate and today's classifier over every entry and diff the verdicts. A source file newly called binary is a regression, not an improvement — it disappears from search and from open. Every disagreement is explained before adoption, not after.
   - **Read cost.** Today is one 512-byte read per entry. A tokenizer-driven chain reads more, and some formats want considerably more (`file-type`'s own stream helper samples 4100 bytes by default; ISO 9660 wants 32 KiB). Cold-build cost is the number that decides this, per [AGENTS.md § Optimization](../AGENTS.md#optimization-and-performance-work).
   - **What the extra formats are worth.** A MIME type per entry is only valuable if a consumer asks for one. Name the consumer or drop the arm — archives, video and documents all land on `binary` today and nothing complains.
   - **The half nothing covers.** Text-versus-binary, encoding, and the `unknown` state stay ours whatever we adopt. If the answer is a dependency _and_ the heuristic, say so and price both.
6. Cost each candidate _index layer_ — language census, symbol index, document graph — on that same repository, cold and warm. Step 5 costs the classifier; this costs the layers above it.
7. Answer the eight questions above in a decision table.
8. Split the result into executable plans, and say explicitly which consumers each unblocks.

Completion: a decision record with numbered decisions, a cost table from a real repository, and a list of the executable plans this becomes.

## What this plan does not do

- No implementation of anything. It produces decisions and follow-up plans.
- No change to the existing index, its watcher, or search behaviour.
- It does not pre-empt [Plan 088](088-native-code-intelligence.md). If that plan's semantic retrieval dictates the storage answer, this plan records that and defers.

## Research findings (2026-09-25)

Read from `origin/main` at `9f3438258`. No open lane branch carries a newer version of this plan. Question 7's census belongs to [Plan 170](170-language-census.md). The one number it needs from here is the fold cost: 0.8 ms on the large repository.

**Method.** The probes run the real index code: `buildWorkspaceIndex`, imported read-only from a checkout whose `apps/server/src/fs/` is identical to `origin/main`. They ran under Bun inside the wave-heavy memory slots on this 28-core machine. Two repositories:

- `references/vscode` at `c1c5b32e3fd`: 23,297 entries, 18,845 files, 246 MB of text.
- This repository: 5,124 entries, 4,504 files.

"Cold" means file contents were dropped from the page cache with `posix_fadvise(DONTNEED)` first. Directory entries stay cached. "Warm" means a repeat run. The probe scripts are in `/work/tmp/research/110/` (scratch).

### Step 1: the index's actual invariants

| #   | Invariant as the code enforces it                                                                                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| I1  | **One index per server process.** It follows the most recently opened root. Opening another root retires the previous index, and reopening the same root does nothing.                                                                                                                                                                                                                                                                                  | `service.ts:670-688` (early return at `:672`)                                                         |
| I2  | **Consumers treat anything except `ready` as absent.** Both search consumers require `readiness === 'ready'` and a matching root, and otherwise fall back to `fd`/`rg`. An enqueued watch event makes the index `stale` until its batch commits, so every save sends search to disk tools for that window. Results stay correct, but the index goes unused during active editing.                                                                       | `search.ts:147-153`, `:196-201`; `workspace-index.ts:609`, `:1153-1165`                               |
| I3  | **`entry.stale` is never true when a consumer reads it.** `ready` is set only when the stale count is 0, so the `entry.stale` checks in search are defensive dead paths.                                                                                                                                                                                                                                                                                | `workspace-index.ts:1324`; `search.ts:210`, `:1021`, `:1051`, `:1071`                                 |
| I4  | **Defect: `ready` while a created path is still pending.** Suppose a create arrives while an earlier batch is still applying. `incrementalStatus` sets `ready` from the stale-entry count, and created paths never mark an entry stale. `pending-probe.ts` got `{readiness: 'ready', pendingCreatedPathCount: 1, hasB: false}`. Name search uses the index in that window and misses the new file.                                                      | `workspace-index.ts:259-264`, `:1312-1329`                                                            |
| I5  | **Defect: only the root `.gitignore` triggers a rebuild.** The matcher also reads nested `.gitignore`, `.ignore` and `.git/info/exclude`, but none of them reclassifies anything. `gitignore-probe.ts` added `sub/.gitignore` with `*.log`: the index stayed `ready` and `sub/gen.log` kept `gitIgnored: false`. Editing the root `.gitignore` fixed it through a full rebuild.                                                                         | `workspace-index.ts:1167-1172`; `search-gitignore.ts:21-22`                                           |
| I6  | **Defect: `failed` is terminal.** After a watch error, `applyWatchEvents` and `refresh` return early. Reopening the same root does not reinstall the index (I1). Only a server restart or another root recovers it. A failed cold scan also clears every entry.                                                                                                                                                                                         | `workspace-index.ts:171-173`, `:182-183`, `:201`, `:266-281`; `service.ts:730-743`                    |
| I7  | **A burst of 500 or more queued events rebuilds from scratch**, and readiness reports `building` while it runs.                                                                                                                                                                                                                                                                                                                                         | `workspace-index.ts:592-596`, `:649-651`                                                              |
| I8  | **Ignored directories are indexed but not descended.** The directory itself gets an entry with `gitIgnored` or `defaultIgnored` set. Its children get none.                                                                                                                                                                                                                                                                                             | `workspace-index.ts:805-811`                                                                          |
| I9  | **The walk is serial.** Each entry awaits its stat, `readdir` and sniff `open`/`read` before the next entry starts.                                                                                                                                                                                                                                                                                                                                     | `workspace-index.ts:711-733`                                                                          |
| I10 | **Only one of three classification paths runs the signatures.** Image and binary extensions settle with no read. Known text extensions read 512 bytes and check only for NUL, so a PNG named `.txt` becomes `binary`, never `image`. Everything else runs `sniffBytes`: signatures, then NUL, then control bytes. The only consumer of the verdict is search, which turns `binary`/`image` into rg exclude globs. `version` is `stat:<mtimeMs>:<size>`. | `workspace-index.ts:817-832`, `:923-937`; `search.ts:215-230`, `:895`, `:1057-1063`; `version.ts:6-8` |

I4, I5 and I6 break the freshness contract that every derived layer would inherit. They make up the first follow-up plan.

### Step 2: prior art on freshness and scale

| System                    | Index                                                                                                                                                                                                      | Freshness contract                                                                                                                                                                       | Evidence                                                                                           |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| VS Code quick open        | Nothing persisted. It runs `rg --files` and caches the list for one picker session (`cacheKey`), dropped when the picker closes.                                                                           | Fresh as of when the picker opened. No watcher.                                                                                                                                          | `rawSearchService.ts:28`, `:170`, `:379`; `anythingQuickAccess.ts:562`; `ripgrepFileSearch.ts:31`  |
| VS Code text search       | None. `rg` runs per query.                                                                                                                                                                                 | Always fresh.                                                                                                                                                                            | `ripgrepTextSearchEngine.ts`                                                                       |
| VS Code workspace symbols | None of its own. Providers answer. For TypeScript that is tsserver `navto` over the loaded program.                                                                                                        | Whatever the language server holds.                                                                                                                                                      | `extensions/typescript-language-features/src/languageFeatures/workspaceSymbols.ts:64`              |
| ripgrep                   | None.                                                                                                                                                                                                      | Always fresh. The cost is paid per query: on vscode, `rg -c TODO` takes 17–20 ms warm and 289 ms content-cold on this machine.                                                           | measured                                                                                           |
| Codex file search         | Nothing persisted. A parallel `ignore::WalkBuilder` walk feeds `nucleo` once per search session.                                                                                                           | Per session.                                                                                                                                                                             | `references/codex` `a0b85c7a`, `codex-rs/file-search/src/lib.rs:7-15`, `:109`                      |
| opencode                  | None. Grep and glob go to `rg`, and symbols go to LSP `workspace/symbol`.                                                                                                                                  | Delegated to those tools.                                                                                                                                                                | `references/opencode` `adee738`, `packages/opencode/src/tool/grep.ts:63`, `src/lsp/lsp.ts:436`     |
| Serena                    | A per-file symbol cache pickled to disk. It maps each path to `(md5 of contents, symbols)`, and each language server contributes a fingerprint.                                                            | Checked lazily on read by rehashing the file. A stale entry is refetched from the language server.                                                                                       | `references/serena` `7a296833`, `src/solidlsp/ls.py:215-219`, `:359-368`, `:553-561`, `:1865-1895` |
| Obsidian `MetadataCache`  | Two layers. The per-file layer holds `links`, `embeds`, `tags`, `headings` and `frontmatter`. The resolved layer holds `resolvedLinks` and `unresolvedLinks`, each mapping source to destination to count. | Each layer has its own events. `changed` fires on a file re-parse (not on rename). `resolve` fires per file. `resolved` fires once everything is resolved and again after later changes. | `references/obsidianmd-obsidian-api` `cc17443`, `obsidian.d.ts:1402-1470`, `:4404-4471`            |

None of these editors or agents keeps a content or path index for correctness. Where an index exists, it takes one of two forms:

- a layer with its own readiness signal (Obsidian);
- a disposable cache keyed by content hash and producer fingerprint (Serena).

### Steps 3–5: `file-type` and the escalation (question 8)

`file-type@22.1.0` is already in the lockfile as a peer of `elysia` (`bun.lock:1806`, `:1904`). Elysia loads it lazily for `t.File`. "Depend" therefore adds a call site and no new package.

**Correctness diff over every file in both repositories.** `classify-probe.ts` compares our verdict with `fileTypeFromBuffer` over the whole file, capped at 16 MiB.

| Finding                                    | vscode                                                                                                                                                                                                                                                    | platform                                                                                                                                                                                                                          |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ours `text`, file-type silent              | 18,455                                                                                                                                                                                                                                                    | 4,472                                                                                                                                                                                                                             |
| Ours `text`, file-type names a type        | 13, all `application/xml` (`.svg`, `.tmLanguage`). Ours is right, so a file-type MIME must never override `text`.                                                                                                                                         | 1 (`.svg`)                                                                                                                                                                                                                        |
| Ours `image`, file-type agrees             | 229 (png 183, ico 32, bmp 14)                                                                                                                                                                                                                             | 21 (jpeg 11, png 8, webp 2)                                                                                                                                                                                                       |
| Ours `image` by extension, content is text | 2 test fixtures (`some.json.png`, `some.xml.png`)                                                                                                                                                                                                         | 0                                                                                                                                                                                                                                 |
| Ours `binary`, file-type names it          | 87: audio/mpeg 43, image/icns 29, font/woff 4, zip (`.nupkg`) 4, dll 4, pdf 1, and 2 PNGs named `.txt`                                                                                                                                                    | 3: audio/mpeg 2, font/ttf 1                                                                                                                                                                                                       |
| **Ours `binary`, but the file is text**    | **7 UTF-16 files (5 with a BOM).** `rg` transcodes BOM'd UTF-16 and finds matches in them. The index's exclude globs (`search.ts:215-230`) drop them only while the index is `ready`, so the same query returns different results depending on readiness. | **1 TypeScript file.** `apps/web/src/features/terminal/state/session-registry.ts:11` has a raw NUL inside a template literal. The index and `rg` both call the file binary, so workspace search cannot find `terminalSessionKey`. |

**Escalation need, measured.** For each non-empty file the probe found the smallest prefix whose file-type verdict matches the whole-file verdict. It tried 512 B, 4,100 B, 32 KiB and the whole file.

- vscode: 18,795 of 18,795 files settle at 512 bytes.
- platform: 4,498 of 4,498 files settle at 512 bytes.

No entry reaches a second tier. Four zip files (`.nupkg`) do exist, but file-type still calls them generic `application/zip` after a full read.

**Cost on vscode (18,845 files):**

| Pass                                                                                    | Warm     | Content-cold |
| --------------------------------------------------------------------------------------- | -------- | ------------ |
| Today's 512-byte read and NUL check                                                     | 43 ms    | 1,553 ms     |
| `fileTypeFromFile` per file (its tokenizer opens and reads)                             | 1,336 ms | 3,022 ms     |
| `fileTypeFromBuffer` on the 512 bytes already read, every file                          | 188 ms   | —            |
| `fileTypeFromBuffer` on those bytes, only for our `binary`/`image` verdicts (327 files) | 2.8 ms   | —            |

**Decisions per arm.**

- **Image arm. Recommendation: leave.** The five signatures cover every image the app can render. Classification has one reader today, search's binary exclusion (I10). Nothing reads the icns, audio, font or pdf MIMEs.
- **MIME per entry. Recommendation: leave, and record how to reopen it.** Reopen it when a consumer asks for a MIME, such as a media preview, "open with" or an agent file listing. Then call the installed `fileTypeFromBuffer` on the existing 512-byte prefix, only after pass one settles `binary`. It must never override `text`. On a vscode-sized build that costs about 3 ms and needs no second read. Never put `fileTypeFromFile` in the index: warm, it costs 31× today's pass.
- **Escalation. Recommendation: a budget of zero.** No consumer needs one, and no entry in either repository would earn a second read. If a consumer reopens it, file-type has already priced the tiers:
  - a 4,100-byte prefix (`reasonableDetectionSizeInBytes`, `index.js:27`);
  - a zip walk capped at 1 MiB per entry and 1,024 entries (`detectors/zip.js:12-13`).

  Pass one's third outcome, "undecided: zip family", gates the walk. The capped `unknown` verdict is stored so the next build skips the read.

- **Text versus binary. Recommendation: keep ours, with two fixes.**
  1. A UTF-16 BOM (`FF FE` or `FE FF`) settles `text`. That check runs after the image signatures and before the NUL scan, which matches `rg` and keeps the ordering above.
  2. The NUL in `session-registry.ts` is a source defect: write the escape. A NUL-bearing file is correctly binary, and `rg` agrees.

### Step 6: cost of each candidate layer

Same two repositories, single-threaded, from `layers-probe.ts`, `index-probe.ts` and `walk-probe.ts`.

| Layer                                                           | vscode cold | vscode warm                        | platform warm | Size                                                                  | Per edited file                     |
| --------------------------------------------------------------- | ----------- | ---------------------------------- | ------------- | --------------------------------------------------------------------- | ----------------------------------- |
| File index (today)                                              | 2,953 ms    | 977–1,075 ms                       | 224–496 ms    | 20.2 MB heap (about 870 B per entry); 9.3 MB as JSON, parsed in 13 ms | one targeted rescan                 |
| ↳ serial stat + 512 B read, files only                          | —           | 504 ms serial, **80 ms at 32-way** | 122 → 19 ms   | —                                                                     | —                                   |
| ↳ gitignore matching                                            | —           | 151 ms                             | 30 ms         | —                                                                     | —                                   |
| `fd -t f`, same tree (reference)                                | 17 ms       | 14 ms                              | 14 ms         | —                                                                     | —                                   |
| Language census (fold over `entryMap()`)                        | 0.8 ms      | 0.8 ms                             | 0.16 ms       | 117 extensions                                                        | O(1) delta                          |
| Document graph (md/mdx links, wiki links, headings, tags)       | 55 ms       | 16 ms                              | 14 ms         | 350 files, 2,676 links (1,105 relative resolved, 445 unresolved)      | p50 0.007 ms, p99 0.46 ms           |
| Syntactic symbols, oxc-parser (TS/JS top level + class members) | 7,494 ms    | 5,994 ms                           | 713 ms        | 13,807 files, 179 MB, **221,470 symbols, 29.7 MB heap**               | p50 0.16 ms, p99 3.9 ms, max 66 ms  |
| Same, TypeScript `createSourceFile`                             | —           | 5,237 ms                           | 713 ms        | 222,711 symbols                                                       | p50 0.15 ms, p99 3.1 ms, max 215 ms |
| Content read floor (every `text` file in full)                  | 1,696 ms    | 86 ms                              | 16 ms         | 246 MB                                                                | —                                   |
| Content search without an index (`rg -c`)                       | 289 ms      | 17–22 ms                           | 7–10 ms       | none                                                                  | none                                |

- **The file index's cost comes from its structure (I9), not from bytes read.** The 512-byte reads take 43 ms warm. A 32-way bounded walk, the concurrency `service.ts` already uses for the tree, cuts the dominant term by about 6×. It is not urgent: the build runs in the background, and search falls back until it finishes.
- **A content index cannot pay for itself at vscode scale.** `rg` answers in 20 ms warm. [`docs/workspace-content-engine-evaluation-plan.md`](../docs/workspace-content-engine-evaluation-plan.md) owns any revisit, which is worth making only for trees about 10× larger or for slow and remote disks.
- **Symbols are the only layer whose cold cost is measured in seconds.** Per edit they cost under a millisecond at p50. The problem is cold start, not freshness.

### Step 7: decision record

| #   | Question                       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | One index or several?          | **Recommendation: one inventory, with separate derived layers.** The file index stays the inventory and the change feed. Each derived layer (census, document graph, symbols) is its own structure, keyed by `(path, version)`, with its own readiness and failure mode. The costs differ by four orders of magnitude (0.8 ms against 6 s), and a parse error in one file must not fail the inventory. Obsidian's `changed`/`resolved` split is the model. Layers subscribe to the index's committed changes and never run a watcher of their own.                                                                                                                                                                                                     |
| D2  | Where does it live?            | **Recommendation: in memory, server-side, per root.** Do not persist the file index: a persisted copy still has to be revalidated with a stat of every entry, and that walk is most of the cost. Persistence would save at most the sniff reads (about 1.5 s cold on vscode) and add a cache format, against the owner's rule from the 2026-09-21 reload-persistence cut: persist intent, not answers. **Do not use `apps/server/src/db`**, which holds durable, migrated product state. The symbol index follows Plan 088 C2: persist only if measurements justify it. vscode's 6–7.5 s cold parse is such a measurement. If it happens, use a disposable file under `PLATFORM_HOME` keyed by content version and parser fingerprint, as Serena does. |
| D3  | Freshness contract             | **Recommendation: every layer exposes `{readiness, generation}`, and consumers query it.** For the file index, `ready` must mean no known pending change (fix I4). Nested ignore files must reclassify their subtree (fix I5). `failed` must be recoverable (fix I6). A derived layer is `ready` only when its input generation equals the index's, and it reports how many files it covers. This follows Plan 088's rule that a read response states its source view and completeness.                                                                                                                                                                                                                                                                |
| D4  | Cheap enough to index eagerly? | **Recommendation:** <ul><li>**Eager:** the inventory (as today) and the census. The census is derived when read and never stored.</li><li>**Built on first ask, then kept incrementally:** the document graph, for Plan 108. It costs 55 ms cold for 350 notes.</li><li>**On demand, in workers:** symbols, owned by Plan 088 C2.</li><li>**Never:** content (step 6).</li></ul>                                                                                                                                                                                                                                                                                                                                                                       |
| D5  | Cost on a large repository     | See the step 6 table (vscode, 23k entries). Every new layer adds its own row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D6  | Who may depend on it?          | **Recommendation: nothing needs the index in order to boot.** Consumers degrade the way search already does (I2): when the index is not `ready`, they keep today's behaviour. Plan 170 preloads every grammar. Agents get an explicit "index not ready", never an empty result, as Plan 088 requires.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D7  | Census first?                  | Yes. The owner split it into [Plan 170](170-language-census.md) on 2026-09-25. Plan 170 needs D3, D6 and the 0.8 ms fold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D8  | Content classification         | **Recommendation: keep ours and leave `file-type`**, as decided per arm above. Fix UTF-16 BOM handling and the NUL in `session-registry.ts`. The steps to reopen MIME detection are recorded above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Owner questions

1. **Should the server index more than one root at a time?** Today it indexes one (I1). Opening a worktree, or a second window on another root, retires the index for the first root. Search there then uses disk tools until the root is reopened. The agent workbench lane gives each session its own worktree, so this will happen more often.
   - (a) Keep one index that follows the latest opened root.
   - (b) One index per open root, LRU-capped. An index costs about 870 B per entry, so vscode takes 20 MB.
   - (c) One index for every root any client or session uses, uncapped.

   **Recommendation: (b), capped at 4, and reinstall a `failed` index when its root is reopened.**

### Proposed follow-up plans

| Plan                                                                  | Scope                                                                                                                                                                                                                                                                            | Unblocks                                                                     |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| [Plan 170](170-language-census.md) (exists)                           | Language census and grammar prefetch.                                                                                                                                                                                                                                            | Grammar and theme prefetch                                                   |
| **Index freshness repairs** (new)                                     | Fix I4, I5 and I6: `failed` recovers on reopen, and a watch error retries with backoff. Settle UTF-16 BOM as `text`. Escape the NUL in `session-registry.ts`. One test per defect. Optional: a 32-way bounded walk, with before and after numbers from `bench:workspace-search`. | Search correctness now, and the contract every layer inherits (D3)           |
| **Per-root indexes** (new, after owner question 1)                    | One index per open root, LRU-capped. Reinstall after `failed`. `info()` reports each root.                                                                                                                                                                                       | Search in worktrees and second windows; Plan 088 agents in session worktrees |
| **Derived-layer contract** (fold into the first layer that needs it)  | A commit listener on `WorkspaceIndex` that reports committed paths with their `version`, and a per-layer `{readiness, generation, covered}`. Not standalone: Plan 170 is a fold and does not need it.                                                                            | Plan 088 C2, the document graph                                              |
| **Document graph** (new, when Plan 108's Obsidian phase is scheduled) | Links, wiki links, headings, tags and frontmatter per md/mdx file. Resolved and unresolved link maps, with Obsidian's two-layer events. In memory, built on first ask.                                                                                                           | Plan 108 backlinks, Plan 088 project memory                                  |
| Symbol index: no new plan                                             | Stays in Plan 088 C2, which already specifies incremental per-file keys. From this plan it takes the cost row (6–7.5 s cold and 30 MB on vscode for TS/JS syntax alone; under a millisecond per edit) and D2's storage answer.                                                   | —                                                                            |
| Content index: no plan                                                | `rg` answers in 20 ms warm on vscode. `docs/workspace-content-engine-evaluation-plan.md` owns any revisit.                                                                                                                                                                       | —                                                                            |
