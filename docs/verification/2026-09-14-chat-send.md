# Send button ignored clicks after typing

A fresh composer rendered Send with `disabled=true`. The draft plugin later set the DOM property to false, but React retained the disabled prop and suppressed the synthetic click. The button looked enabled and did nothing. This was reproduced while driving the real app's browser demo.

React now owns the button's disabled state. The leaf selects one boolean covering prompt text, attachments and terminal context. Removed the imperative button mutation and forwarded ref plumbing.

Before: `/work/tmp/fregat-evidence/20260914T160044Z-scenario-demo-agent-git/`. Read `failure.png`: the typed prompt remains after clicking Send. Requests and client logs show no dispatch or reported error. The focused reproduction also records the ignored click at `/work/tmp/fregat-demo-checks/send-before.txt`.

After: `/work/tmp/fregat-evidence/20260914T160932Z-scenario-demo-agent-git/`. The same scenario, including the Send click, completes. Read `05-agent-reply.png`: the draft cleared and the simulated reply rendered. There are no browser problems, unhandled demo requests or error-level client logs.

Fifteen focused tests pass, including empty-to-content enablement, a working click, additional characters without another React commit, and clearing back to disabled. Results are in `/work/tmp/fregat-demo-checks/send-after.txt`; typecheck and lint outputs are beside it.
