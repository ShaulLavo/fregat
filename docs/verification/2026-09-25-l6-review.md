# L6 final review

The lane was rebased onto the owner-directed rollback at `638137c66`. Its shared helpers and
Query ownership changes preserve the retained L2 behavior. The Git-caller follow-up for reverted
clone, publish, setup and worktree PR features was omitted. Main's empty plan index was retained.

The lane owner performed a read-only diff review of resource disposal, settings recovery,
history paging, queues, filesystem writes, Git lookup ownership, shared policies and TUI state.
The accepted finding was that an atomic staging write removed an existing temporary file when
exclusive creation failed with `EEXIST`. Both the staging API and the complete async writer
reproduced the deletion. They now preserve the colliding path, while a simulated partial disk
write still cleans up its own file. The synchronous writer follows the same collision policy.

The regression file is `apps/server/src/fs/tests/atomic-write.test.ts`. Before the fix, two
preservation cases failed with `ENOENT`; after it, all 12 cases passed. Captures are
`/work/tmp/l6-completion/review-r1-before.log` and `review-r1-after.log`.

Rebase checks also restored the `TurnId` import needed by main's turn-completion helpers, retained
main's quiet approval/turn activity kinds in the shared policy, and added the new Query gate to
the repository gate inventory test. All workspace typechecks passed after those reconciliations.
The script suite passed 111 tests; server passed 2,035 with one skipped; web passed 4,285 with
five skipped; TUI passed 393. All shared-package suites and the eight desktop tests passed. The
final atomic-write, workspace-edit and settings run passed 194 tests after the review fix. Lint,
formatting, generated artifacts, unused-code and repository gates passed. The full test log has
MSW unhandled-request messages and TUI listener-count warnings despite the passing verdicts.
The final browser checks passed with isolated API state on port 33560 and this worktree's Vite
server on port 5216. `chat-stream` proved the expanded output follows its end on command
completion; `chat-history-pages` restored the original prompt after reload and retained
`HISTORY_ROW_229`. Screenshots were read. Evidence is under
`/work/tmp/fregat-evidence/20260925T171305Z-scenario-chat-stream` and
`/work/tmp/fregat-evidence/20260925T171323Z-scenario-chat-history-pages`. Neither run captured a
page error, console error or failed response. Git lookup warnings, navigation-aborted requests,
socket reconnect warnings and GPU diagnostics remain in the captures. CI results are recorded
in the PR when they settle.

Plan 132 P4 remains held. Its [preflight](2026-09-25-schema-collapse-preflight.md) records exact
local paths, data-loss categories and the reversible procedure for a later owner decision.

The atomic helper uses a dedicated `@workspace/contracts/error-fields` export because the Vite
config imports it through the save marker. A root-package import pulled unrelated extensionless
TypeScript imports into Node config loading. Node successfully loaded the actual Vite config
after the import was narrowed.

The first CI run found four new test cases using `Promise.withResolvers`, unavailable in the
runner's Node 20. The settings recovery file reproduced three failures and the Shiki lifetime
file one failure on local Node 20.20.2. Explicit promise resolvers preserve the controlled races;
all nine cases passed afterward on the same runtime. Evidence is in
`/work/tmp/l6-completion/node20-settings-before.log`, `node20-markdown-before.log` and
`node20-tests-after.log`. Application behavior was unchanged.

That CI run also caught the retained child-approval test stopping its fake Codex process after
one response while asserting two. It now waits for both recorded responses before stopping.
The failure is in `/work/tmp/l6-completion/ci-server.log`; all 68 adapter tests passed afterward
in `codex-child-responses-after.log`. The other eight CI jobs passed on that run.

The second CI run passed the corrected package and server jobs and exposed two further cases:

- Concurrent TUI opens returned `SQLITE_BUSY_RECOVERY`, which the exact `SQLITE_BUSY` check
  rejected. Initialization now checks SQLite's primary result code while keeping the existing
  retry deadline. [SQLite documents the primary code in the low eight bits](https://sqlite.org/rescode.html#primary_result_codes_versus_extended_result_codes).
  The existing real reader-lock test now also maps the driver's immutable lock error to the
  recovery variant captured in CI. That case failed before and passed after the change; all 16
  storage cases passed, including real simultaneous writers and persistent-lock preservation.
- The chat correction test left its final optimistic message pending. Its diagnostic timer
  outlived the test document. The test now resets its message intents during cleanup, and an
  after-test assertion checks that no active intent remains. That assertion reproduced the leak
  before the fix. All five chat-view and hold-diagnostic cases passed afterward.

Those failures are captured in `ci-tui.log`, `ci-web3.log`, `busy-recovery-before.log` and
`chat-intent-cleanup-before.log` under `/work/tmp/l6-completion`. The combined passing tests and
TUI/web typechecks are in `ci-contention-cleanup-after.log`. All database checks used disposable
fixtures; the schema-collapse hold and real database files were untouched.
